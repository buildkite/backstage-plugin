services:
  bun:
    image: oven/bun:latest
    working_dir: /app
    volumes:
      - .:/app
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - NODE_ENV=test
    entrypoint: ["bun"]
